import React from "react";

export default function Task(){
    return (
        <div>
            <input type="checkbox"/>
            <span>Task</span>
            <button>Delete</button>
        </div>
        )
}